<%= form_for @gemfile, url: team_project_gemfile_path do |f| %>

  <% if @gemfile.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@gemfile.errors.count, "error") %> prohibited this gemfile from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @gemfile.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :context, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :context, :class => 'form-control' %>

    </div>
    <%=f.error_span(:context) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
    </div>
  </div>

<% end %>



      <%= html @gemfile.context %>
